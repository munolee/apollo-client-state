import { FC } from 'react'
import styled from "styled-components";

const TodoList: FC = () => {

    return (<TodoContainer>test</TodoContainer>)
}

const TodoContainer = styled.div`
  
`

export default TodoList